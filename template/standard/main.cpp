#include <iostream>

int main() {
    using namespace std;
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    cout << 2;

    for(int i = 0; i < 100; ++i) {
        cout << i << endl;
    }
    
    return 0;
}
